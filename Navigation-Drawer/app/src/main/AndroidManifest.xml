<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.navigation">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".MainActivity" />
        <activity
            android:name=".ViewVideos.ExtendedListVideos.RecycleView"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewVideos.ListVideos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewFolders.ListVideoDates"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ViewVideos.ViewVideo" />
        <activity android:name=".Servo.Servo" />
        <activity android:name=".Livestream.Livestream" />
        <activity android:name=".Authentication.FirebaseAuthentication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>